import { DateCommentPipe } from './date-comment.pipe';

describe('DateCommentPipe', () => {
  it('create an instance', () => {
    const pipe = new DateCommentPipe();
    expect(pipe).toBeTruthy();
  });
});
