import { DateActivityPipe } from './date-activity.pipe';

describe('DateActivityPipe', () => {
  it('create an instance', () => {
    const pipe = new DateActivityPipe();
    expect(pipe).toBeTruthy();
  });
});
